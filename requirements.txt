# 智能体 Python 依赖（非 GPU 部分）
pyyaml
openai              # 调用国产大模型（DeepSeek/Qwen/GLM/Kimi/豆包，OpenAI兼容接口）
# anthropic         # 仅当用 Claude 时需要（需美元卡）
yt-dlp              # 第1步 YouTube/B站下载
f2                  # 第1步 抖音下载
# 兜底语音合成（无 GPU/无 API 也能出音轨）
edge-tts            # 在线，最自然（部分环境被墙时自动退回 espeak）
# 离线兜底需系统装 espeak-ng： sudo apt-get install -y espeak-ng
# 第2步 转写（按需装其一）
faster-whisper      # 无 GPU 兜底（CPU）
# funasr modelscope # 有 GPU 推荐，中文最佳
openai              # 可选：Whisper API 转写
# 第6步 分发
playwright          # social-auto-upload 依赖；装后需 playwright install chromium

# GPU 部分（在 GPU 实例上单独装，见各目录脚本）：
#   04-voice-clone: index-tts（IndexTTS2）+ torch(cuda)
#   05-render-remotion/latentsync.py: LatentSync + torch(cuda)
